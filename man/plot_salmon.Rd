% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_salmon.R
\name{plot_salmon}
\alias{plot_salmon}
\title{Plot salmon biomass, abundance, and weight violin plots}
\usage{
plot_salmon(
  biom_file = system.file("extdata", "biom_comp_run_status_quo.csv", package =
    "statusquoscenarios"),
  naa_file = system.file("extdata", "NAA-WAA_sim.csv", package = "statusquoscenarios"),
  fg_file = system.file("extdata", "PugetSoundAtlantisFunctionalGroups_2024_V1_SQ.csv",
    package = "statusquoscenarios"),
  baseline_year = "Yr2011",
  yr_min = 40L,
  output_dir = "output",
  output_file = "Salmon_plots.pdf"
)
}
\arguments{
\item{biom_file}{Character. Path to the biomass CSV. Defaults to bundled.}

\item{naa_file}{Character. Path to the NAA/WAA CSV. Defaults to bundled.}

\item{fg_file}{Character. Path to the functional groups CSV. Defaults to
bundled.}

\item{baseline_year}{Character. Baseline \code{run_name} / \code{scenario}. Default
\code{"Yr2011"}.}

\item{yr_min}{Integer. Minimum \code{Year} (exclusive). Default \code{40L}.}

\item{output_dir}{Character. Output directory. Created if absent. Default
\code{"output"}.}

\item{output_file}{Character. PDF filename. Default \code{"Salmon_plots.pdf"}.}
}
\value{
Invisibly returns the path to the saved PDF.
}
\description{
Produces a PDF with six panels:
\enumerate{
\item All fish groups (biomass relative change) — violin by \code{Large_group}.
\item Salmon only (biomass) — violin by species subgroup (n labels included).
\item Salmon excluding "Other salmon" (biomass).
\item Salmon abundance-at-age (NAA) — violin by species.
\item Salmon NAA excluding "Other salmon".
\item Salmon weight-at-age (WAA) — violin by species.
}
}
\examples{
\dontrun{
plot_salmon(output_dir = "output")
}
}
