% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ssm_spatial.R
\name{plot_ssm_spatial}
\alias{plot_ssm_spatial}
\title{Plot spatial maps of SSM output differences between decades}
\usage{
plot_ssm_spatial(
  data_dir,
  bgm_file = system.file("extdata", "PugetSound_89b_NAD83.bgm", package =
    "statusquoscenarios"),
  variables = list(temperature = "temperature", salinity = "salinity", oxygen = "Oxygen",
    NH4 = "NH3", NO3 = "NO3", SP = "Sm_Phyto_N", LP = "Lrg_Phyto_N"),
  years = c(2020L, 2030L, 2040L),
  layer = c("full", "surface", "bottom"),
  output_dir = "output",
  output_file = NULL
)
}
\arguments{
\item{data_dir}{Character. Root directory containing per-year subdirectories
with NetCDF files following the naming convention
\verb{\{year\}/pugetsound_SSM_Atlantis_\{variable\}_status_quo_\{year\}.nc}.}

\item{bgm_file}{Character. Path to the Puget Sound \code{.bgm} geometry file.
Defaults to the bundled copy in \code{inst/extdata}.}

\item{variables}{A named list mapping NetCDF file variable names to Atlantis
variable names (the name inside the NetCDF), e.g.
\code{list(temperature = "temperature", NH4 = "NH3")}.
Defaults to the seven standard SSM variables.}

\item{years}{Integer vector of future scenario years to compare against the
2011 baseline. Defaults to \code{c(2020, 2030, 2040)}.}

\item{layer}{Character. One of \code{"full"} (volume-weighted depth average),
\code{"surface"} (topmost active layer), or \code{"bottom"} (layer 1). Default
\code{"full"}.}

\item{output_dir}{Character. Directory where the PDF will be saved. Created
if it does not exist. Defaults to \code{"output"}.}

\item{output_file}{Character. PDF filename (without path). Default depends on
\code{layer}: \code{"Spatial.pdf"}, \code{"Spatial_surface.pdf"}, or
\code{"Spatial_bottom.pdf"}.}
}
\value{
Invisibly returns the path to the saved PDF.
}
\description{
Creates a multi-page PDF with 3-panel spatial delta maps (difference from the
2011 baseline) for each variable × decade combination. Three layer modes are
supported: depth-averaged (\code{"full"}), bottom layer (\code{"bottom"}), and surface
layer (\code{"surface"}).
}
\examples{
\dontrun{
plot_ssm_spatial(
  data_dir  = "/path/to/nc/data",
  layer     = "full",
  output_dir = "output"
)
}
}
