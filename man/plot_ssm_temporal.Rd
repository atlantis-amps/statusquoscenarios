% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ssm_temporal.R
\name{plot_ssm_temporal}
\alias{plot_ssm_temporal}
\title{Plot temporal time series of SSM outputs across decades}
\usage{
plot_ssm_temporal(
  data_dir,
  bgm_file = system.file("extdata", "PugetSound_89b_NAD83.bgm", package =
    "statusquoscenarios"),
  variables = list(NO3 = list(nc = "NO3", atl = "NO3", ylab = "NO3 (mgN/m3)", ylim =
    c(250, 600), hline = c(250, 350, 450, 550)), NH4 = list(nc = "NH4", atl = "NH3", ylab
    = "NH4 (mgN/m3)", ylim = c(5, 35), hline = c(10, 20, 30, 40)), SP = list(nc = "SP",
    atl = "Sm_Phyto_N", ylab = "Small phytoplankton (mgN/m3)", ylim = c(0, 9), hline =
    c(2, 4, 6, 8)), LP = list(nc = "LP", atl = "Lrg_Phyto_N", ylab =
    "Large phytoplankton (mgN/m3)", ylim = c(0, 30), hline = c(10, 20, 30, 40)),
    temperature = list(nc = "temperature", 
     atl = "temperature", ylab =
    "Temperature (°C)", ylim = c(8, 15), hline = c(9, 11, 13, 15)), oxygen = list(nc =
    "oxygen", atl = "Oxygen", ylab = "Oxygen", ylim = c(5000, 8000), hline = c(5000,
    6000, 7000, 8000)), salinity = list(nc = "salinity", atl = "salinity", ylab =
    "Salinity", ylim = c(28.5, 31), hline = c(28.5, 29.5, 30.5, 31.5))),
  years = c(2020L, 2030L, 2040L),
  layer = c("full", "bottom"),
  output_dir = "output",
  output_file = NULL
)
}
\arguments{
\item{data_dir}{Character. Root directory with per-year NetCDF subdirectories.}

\item{bgm_file}{Character. Path to the \code{.bgm} file. Defaults to the bundled
copy in \code{inst/extdata}.}

\item{variables}{A named list mapping NetCDF variable names to Atlantis
variable IDs. Defaults to the seven standard SSM variables.}

\item{years}{Integer vector of future scenario years. Defaults to
\code{c(2020, 2030, 2040)}.}

\item{layer}{Character. \code{"full"} (volume-weighted over all layers) or
\code{"bottom"} (layer 1 only). Default \code{"full"}.}

\item{output_dir}{Character. Output directory. Created if absent. Default
\code{"output"}.}

\item{output_file}{Character. PDF filename. Defaults to
\code{"temporal_mean.pdf"} or \code{"temporal_mean_bottom.pdf"} depending on
\code{layer}.}
}
\value{
Invisibly returns the path to the saved PDF.
}
\description{
Creates a PDF with base-R line plots showing volume-weighted (or
bottom-only) time series of oceanographic variables for each scenario
decade. A total-phytoplankton panel (SP + LP) is appended automatically.
}
\examples{
\dontrun{
plot_ssm_temporal(
  data_dir  = "/path/to/nc/data",
  layer     = "full",
  output_dir = "output"
)
}
}
