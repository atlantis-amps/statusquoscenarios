% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_all_analyses.R
\name{run_all_analyses}
\alias{run_all_analyses}
\title{Run all AMPS status quo analyses}
\usage{
run_all_analyses(
  data_dir,
  bgm_file = system.file("extdata", "PugetSound_89b_NAD83.bgm", package =
    "statusquoscenarios"),
  biom_file = system.file("extdata", "biom_comp_run_status_quo.csv", package =
    "statusquoscenarios"),
  naa_file = system.file("extdata", "NAA-WAA_sim.csv", package = "statusquoscenarios"),
  fg_file = system.file("extdata", "PugetSoundAtlantisFunctionalGroups_2024_V1_SQ.csv",
    package = "statusquoscenarios"),
  baseline_year = "Yr2011",
  yr_min = 40L,
  years = c(2020L, 2030L, 2040L),
  output_dir = "output"
)
}
\arguments{
\item{data_dir}{Character. Root directory with per-year NetCDF
subdirectories. Required for SSM spatial/temporal functions.}

\item{bgm_file}{Character. Path to the \code{.bgm} file. Defaults to bundled.}

\item{biom_file}{Character. Path to the biomass CSV. Defaults to bundled.}

\item{naa_file}{Character. Path to the NAA/WAA CSV. Defaults to bundled.}

\item{fg_file}{Character. Path to the functional groups CSV. Defaults to
bundled.}

\item{baseline_year}{Character. Baseline scenario label shared by biomass /
salmon / histogram functions. Default \code{"Yr2011"}.}

\item{yr_min}{Integer. Minimum \code{Year} (exclusive). Default \code{40L}.}

\item{years}{Integer vector of future scenario years for SSM functions.
Default \code{c(2020L, 2030L, 2040L)}.}

\item{output_dir}{Character. Directory where all PDFs are saved. Created if
absent. Default \code{"output"}.}
}
\value{
Invisibly returns a named character vector of all output PDF paths.
}
\description{
Convenience wrapper that calls all six plot functions in sequence, saving
every output PDF to \code{output_dir}.
}
\examples{
\dontrun{
run_all_analyses(
  data_dir   = "/path/to/nc/data",
  output_dir = "output"
)
}
}
